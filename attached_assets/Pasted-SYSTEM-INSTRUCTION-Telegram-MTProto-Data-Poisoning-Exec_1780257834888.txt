SYSTEM INSTRUCTION: Telegram MTProto Data Poisoning Execution
MISSION CONTEXT:
You are tasked with setting up and executing a local Python script (⁠data_poisoning.py⁠) that uses the ⁠Telethon⁠ library. This script acts as a Telegram Userbot. Its purpose is to connect to a target bot (⁠@pntcclub3_bot⁠), cycle through its conversational UI, and inject randomized garbage strings into the bot's database to overwrite and permanently poison existing profile metadata tied to the user's account.
THE SCRIPT:
The user has already provided the complete, working ⁠data_poisoning.py⁠ script. Your job is to facilitate its execution, handle the environment setup, and troubleshoot any runtime anomalies.
PHASE 1: Environment & Authentication Setup
1 Dependencies: Ensure the environment has Python 3.8+ and install the required library by running ⁠pip install telethon⁠.
2 Credentials: The script requires a Telegram ⁠API_ID⁠ and ⁠API_HASH⁠ (obtained from my.telegram.org).
 Prompt the user to provide these securely, OR instruct them to set them as environment variables (⁠TG_API_ID⁠ and ⁠TG_API_HASH⁠).
 Do not log these credentials to the console.
PHASE 2: The Interactive Auth Flow (CRITICAL)
The first time ⁠await client.start()⁠ runs, Telethon will require interactive terminal input. You must prepare the user for this:
1 It will ask for the user's phone number (in international format, e.g., ⁠+55...⁠).
2 It will ask for the login code sent to their Telegram app.
3 If the user has 2FA enabled, it will ask for their cloud password.
Agent Directive: If you are running this autonomously in a non-interactive shell, you must establish a secure prompt/pipe to feed these inputs to the script's ⁠stdin⁠.
PHASE 3: Execution & Monitoring
Run the script using ⁠python data_poisoning.py⁠. Monitor the standard output for the following edge cases and handle them accordingly:
 Failure Case 1: "Could not find or click the Edit button"
 Cause: The target bot may have updated its UI, or the user's locale changed the button text from ⁠✏️ Editar perfil⁠.
 Fix: Intercept the raw ⁠menu_msg⁠ JSON, extract the correct button text or index, and update line 52 (⁠await menu_msg.click(text='...')⁠) in the script.
 Failure Case 2: ⁠FloodWaitError⁠
 Cause: Telegram's anti-spam API is temporarily rate-limiting the account.
 Fix: The script natively catches this and waits. Do not restart the script; allow it to sleep for the requested duration.
 Failure Case 3: ⁠TimeoutError⁠
 Cause: The target bot (⁠pntcclub3_bot⁠) is offline, lagging, or has shadow-banned the user.
 Fix: Increase the timeout parameter in ⁠async with self.client.conversation(..., timeout=15)⁠ to 30 seconds.
SUCCESS CRITERIA
The script must successfully complete 30 iterations of injecting 150-400 character randomized payloads. Once the console logs ⁠"Poison sequence complete. Original metadata has been overwritten."⁠, the task is successfully concluded. Proceed immediately.